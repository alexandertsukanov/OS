#include <cmath>
#include "iostream"
#include "RSA.h"
#include <windows.h>

	int main(){

		
		
		float a = 70;
		float b = RSAImpl::Crypt(a);
		float c = rsa.Decrypt(b);
		printf("%d\n", (int) b);
		printf("%d\n", (int) c);

	}
